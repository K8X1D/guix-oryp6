(load "../gtransform.scm")
(packages->manifest
 (fixpkgs
 '(
    ;; browsers
    (raw "qutebrowser")
    (raw "firefox")
    "nyxt"

    ;;(raw "rstudio")
    ;;"rstudio"
    (raw "gimp")
    "libreoffice"
    "texmacs"

)))

